# images/

Dépose ici tes photos, nommées comme dans `data/photos.geojson`
(par ex. `001.jpg`, `002.jpg`...).

Si une image est absente, le cadre garde son fond dégradé de secours
défini dans `css/style.css` (`.photo`).
